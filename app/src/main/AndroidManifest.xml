<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Created by Nicanor Orlando.
  ~ Copyright (c) 7/12/21 09:33.
  ~ All rights reserved.
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.institutomisionero.IM">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/icono_im"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ImagenCompleta"
            android:parentActivityName=".MenuSlideActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".PDF"
            android:parentActivityName=".MenuSlideActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity
            android:name=".MenuSlideActivity"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".loginActivity"
            android:theme="@style/AppTheme.PopupOverlay" />

        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.PopupOverlay" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>